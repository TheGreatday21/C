#include <ctype.h>
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
    //bigO(upper bound ) tends to the worst case most times but the lower bound(best case) of algorithms is omega(Ω)
//O(n^2) slowest
//O(n log n)
//O(n) Linear search 
//O(log n) Binary search 
//O(1) fastest 

//Ω(n^2) slowest
//Ω(n log n)
//Ω(n)  
//Ω(log n) 
//Ω(1) Linear search- the item is the first 

//THe coincidence of both upper bound and lower bound being the same we use ɵ

//Linear search with simple numbers array 
/*
    int n, numbers[] = {10,20,40,500,50,30};

    printf("This is an array of numbers from which you are to choose the number you want to find :\n  {10,20,40,500,50,30} \n");
    printf("Which number do you wish to find :");
    scanf("%d",&n);

    for(int i = 0; i < 6; i ++)
    {
        if (numbers[i] == n)
        {
            printf("Found at index %d \n",i);
            return 0;
        }

    }
    printf("Not found\n");

    return 1;
*/

//Linear search with a string array 
    
    for (int i = 0 ; i < 7 ; i ++)
        {

        }   


}

//in the terminal you run the command (echo $?) which in most cases returns 0











