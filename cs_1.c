#include <cs50.h>
#include <stdio.h>

int main(void){
	//Here we are learning to ouptut information to the screen 
	printf("\\n \n");
	printf("Hi my name is Elijah the Greatest programmer thats ever lived in Jinja Uganda \n");
	
	//Here we are learning to get input from the users
	string answer = get_string("What is your name lad ? ");
	//in C you have to specify the type of data the variable is to store either integer , string or float 
	printf("nice to meet you, %s\n", answer );
	//we use %s if we want a string of text to be added to our string in the printf statement. It acts like a join function to join our string from the varibale to the string in the printf statement. 
	 
}







